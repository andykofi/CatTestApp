import React from 'react';
import Wrapper from "../components/Wrapper";

const Upload = () => {
    return (
        <Wrapper>
            Cat Upload page
        </Wrapper>
    );
};

export default Upload;
